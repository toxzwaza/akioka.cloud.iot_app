# アキオカ 物品管理システム（akioka.cloud.iot_app）仕様書

## 1. システム概要

### 1.1 基本情報

| 項目 | 内容 |
|------|------|
| システム名 | アキオカ物品管理システム |
| 本番URL | https://akioka.cloud |
| フレームワーク | Laravel 9.x + Inertia.js（フロントエンド連携） |
| PHP | 8.0.2 以上 |
| データベース | MySQL（akioka_db / ポート3308） |
| 認証 | Laravel Breeze |
| API認証 | Laravel Sanctum |
| フロントエンド | Inertia.js（React/Vue）+ Ziggy（ルート生成） |
| PWA | manifest.json + Service Worker 対応 |

### 1.2 主要パッケージ

| パッケージ | 用途 |
|-----------|------|
| inertiajs/inertia-laravel | SPA風フロントエンド統合 |
| laravel/sanctum | APIトークン認証 |
| barryvdh/laravel-dompdf | PDF生成 |
| google/auth + chrome-php/chrome | Firebase認証・プッシュ通知 |
| guzzlehttp/guzzle | HTTP通信（Teams Webhook等） |
| tightenco/ziggy | LaravelルートのJavaScript利用 |

---

## 2. システム構成図

```
[タブレット端末]  →  [Laravel Webアプリ]  →  [MySQL DB]
  (PWA対応)           ├── Inertia.js          akioka_db
                      ├── Teams Webhook通知
                      ├── Firebase Cloud Messaging
                      └── 外部API連携

[管理用PC]  →  [承認画面・納品画面・管理画面]

[サイネージモニター]  →  [コンテンツ表示画面]
  ├── 環境データ表示（温度・湿度・CO2）
  ├── 施設使用状況
  └── 納品完了表示
```

---

## 3. 機能一覧

### 3.1 物品管理（在庫管理）

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| メイン画面 | `GET /` | MainController@index | トップ画面（タブレット用） |
| 在庫検索 | `GET /search` | SearchController@index | 物品名・品番で検索 |
| 検索結果 | `GET /search/result` | SearchController@result | 検索結果一覧 |
| 在庫詳細 | `GET /stock/{stock_id}/{stock_storage_id}` | InventoryController@show | 在庫の詳細情報表示 |
| 在庫登録 | `POST /stock` | InventoryController@store | 新規在庫の登録 |
| 数量変更 | `POST /stock/changeQuantity` | InventoryController@changeQuantity | 在庫数量の手動変更 |
| 画像管理 | `POST /stock/updateFile` / `DELETE /stock/deleteImage` | InventoryController | 在庫画像のアップロード・削除 |
| 略名管理 | `POST /stock/createAlias` / `PUT /stock/editAlias` / `POST /stock/deleteAlias` | StockAliasController | 在庫の別名管理 |
| 格納先管理 | `POST /stock/createStockStorage` / `DELETE /stock/deleteStockStorage` | InventoryController | 格納先アドレスの追加・削除 |
| 出庫 | `POST /shipment` | ShipmentController@store | 在庫の出庫処理 |

### 3.2 発注依頼

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 発注依頼作成 | `POST /order` | OrderController@store | 物品の発注依頼を作成 |
| 発注依頼削除 | `DELETE /order/{id}` | OrderController@delete | 発注依頼の取消 |
| 確定発注取得 | `GET /order/getConfirmOrderRequest` | OrderController@getConfirmOrderRequest | 承認済み発注の一覧取得 |
| 稟議書アップロード | `POST /api/order_request/upload_file` | OrderRequestController@uploadFile | 発注に関する稟議書PDF添付 |
| 再依頼 | `POST /order_request/reorder` | OrderRequestController@reorder | 過去の依頼から再発注 |

### 3.3 承認ワークフロー

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 承認画面 | `GET /accept/order-request` | AcceptController@index | 承認待ち発注依頼一覧 |
| 承認/却下 | `PUT /accept/order-request` | AcceptController@update | 個別の承認・却下処理 |
| 一括承認/却下 | `PUT /accept/order-request/bulk` | AcceptController@bulkUpdate | 複数件の一括処理 |
| 部署別承認済み一覧 | `GET /accept/department-approved-items` | AcceptController@getDepartmentApprovedItems | 部署別の承認済み物品集計 |

### 3.4 納品管理

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 納品トップ | `GET /receive` | ReceiveController@home | 納品管理メニュー |
| 未登録注文一覧 | `GET /receive/getInitialOrders` | ReceiveController@getInitialOrders | 納品書未登録の注文 |
| 納品書アップロード | `POST /receive/uploadFile` | ReceiveController@uploadFile | 納品書ファイル登録 |
| 納品登録画面 | `GET /receive/delivery/{id}` | ReceiveController@delivery | 個別納品の登録画面 |
| 納品数量登録 | `GET /receive/updateDelivery` | ReceiveController@updateDelivery | 納品数量の確定・在庫加算 |
| 受領確認 | `GET /receive/updateReceipt/{id}` | ReceiveController@updateReceipt | 現場への引き渡し完了 |
| 新規在庫登録 | `POST /receive/store` | ReceiveController@store | 未登録物品の在庫マスタ登録 |

### 3.5 現場物品依頼

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 依頼画面 | `GET /requests` | StockRequestController@home | 現場からの物品依頼画面 |
| 依頼登録 | `POST /requests/store` | StockRequestController@store | 物品依頼の作成 |
| 払出完了 | `POST /requests/complete` | StockRequestController@complete | 在庫払出の完了処理 |
| 不足分発注 | `POST /requests/order` | StockRequestController@order | 在庫不足時の自動発注依頼 |
| 依頼削除 | `DELETE /requests/delete` | StockRequestController@delete | 依頼の取消 |

### 3.6 新規品依頼

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 新規品依頼画面 | `GET /new_item` | NewItemController@home | 未登録物品の依頼画面 |
| 新規品登録 | `POST /stock/new-item/store` | NewItemController@store | 新規物品の登録 |

### 3.7 物品依頼確認

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 確認画面 | `GET /check_order_request` | CheckOrderRequestController@index | 発注依頼の状況確認 |
| 依頼一覧取得 | `GET /check_order_request/getOrderRequests` | CheckOrderRequestController@getOrderRequests | 依頼データ一覧 |
| 詳細表示 | `GET /check_order_request/show/{id}` | CheckOrderRequestController@show | 個別依頼の詳細 |

### 3.8 滞留在庫管理

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 滞留画面 | `GET /retentions` | RetentionController@home | 長期滞留在庫の監視 |
| 滞留在庫取得 | `GET /retentions/stocks/getRetentionStocks` | RetentionController@getRetentionStocks | 滞留在庫データ |

### 3.9 弁当注文

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 弁当画面 | `GET /lunch` | LunchController@index | 弁当注文トップ |
| 注文 | `POST /lunch/order` | LunchController@order | 弁当の注文 |
| 受取 | `POST /lunch/receive` | LunchController@receive | 弁当の受取確認 |

### 3.10 IoT・環境モニタリング

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 熱中症ダッシュボード | `GET /dashboard` | HeatStrokeController@dashboard | 温湿度監視 |
| 工場環境マップ | `GET /watchData` | ContentController@watchData | 温度・湿度・CO2プロット |
| 作業場環境モニター | `GET /factoryEnvMonitor/{place_id}` | ContentController@factoryEnvMonitor | 場所別環境データ |
| 施設使用状況 | `GET /facilitySchedule` | ContentController@facilitySchedule | 施設予約状況 |

### 3.11 サイネージ表示

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| 納品完了（食堂裏） | `GET /receive/complete/back` | ContentController@receiveCompleteBack | 食堂モニター用 |
| 納品完了（現場） | `GET /receive/complete` | ContentController@receiveComplete | 現場モニター用 |
| 物品依頼表示 | `GET /order-request/complete` | ContentController@orderRequestComplete | 発注依頼モニター |

### 3.12 デバイス管理

| 機能 | ルート | コントローラ | 説明 |
|------|--------|-------------|------|
| デバイスメッセージ取得 | `GET /device-message` | DeviceMessageController@getDeviceMessages | 端末通知の取得 |
| メッセージ確認 | `POST /device-message` | DeviceMessageController@confirm_message | メッセージの既読処理 |
| デバイスログイン | `POST /device-login` | DeviceController@store | タブレット端末の登録 |
| 最終アクセス更新 | `POST /device-update-access` | DeviceController@updateLastAccess | アクセス日時の記録 |

---

## 4. データベース設計

### 4.1 主要テーブル

#### stocks（物品マスタ）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_no | varchar | 品番 |
| name | varchar | 品名 |
| s_name | varchar | 品番・規格名 |
| img_path | varchar | 画像パス |
| url | varchar | 商品URL |
| price | decimal(10,2) | 単価 |
| tax_included | boolean | 税込フラグ |
| solo_unit | varchar | 単品単位（個・本等） |
| org_unit | varchar | 梱包単位（箱・袋等） |
| quantity_per_org | int | 梱包入数 |
| deli_location | varchar | 納品場所 |
| classification_id | int | 分類ID |
| not_stock_flg | boolean | 在庫なし管理フラグ |
| jan_code | varchar | JANコード |
| main_unit_flg | int | 主単位フラグ |
| price_check_flg | int | 価格確認フラグ |
| approval_supplier_name | varchar | 承認仕入先名 |
| special_area_cd | varchar | 特殊エリアコード |
| del_flg | int | 削除フラグ |
| memo | text | メモ |
| desc_memo | text | 説明メモ |
| show_price_on_invoice | int | 納品書価格表示フラグ |

#### order_requests（発注依頼）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_id | bigint | FK → stocks |
| user_id | bigint | 発注担当者ID |
| request_user_id | bigint | 依頼者ID |
| supplier_id | bigint | FK → suppliers |
| device_id | bigint | 依頼元端末ID |
| document_id | bigint | FK → documents（稟議書） |
| quantity | int | 必要数量 |
| unit | varchar | 数量単位 |
| now_quantity | int | 現在数量 |
| now_quantity_unit | varchar | 現在数量単位 |
| price | decimal | 単価 |
| calc_price | decimal | 合計金額（単価×数量） |
| postage | decimal | 送料 |
| lead_time | int | リードタイム（日） |
| desire_delivery_date | date | 希望納期 |
| digest_date | date | 消化予定日 |
| description | text | 依頼者備考 |
| sub_description | text | 発注者備考 |
| file_path | varchar | 稟議書ファイルパス |
| status | int | ステータス（0:未確定, 1:確定） |
| accept_flg | int | 承認状態（後述） |
| new_stock_flg | int | 新規品フラグ |
| del_flg | int | 削除フラグ |

**accept_flg の値:**

| 値 | 状態 |
|----|------|
| 0 | 未申請（初期値） |
| 1 | 承認申請中 |
| 2 | 最終承認済み |
| 3 | 却下 |
| 6 | 差し戻し |

#### order_request_approvals（承認履歴）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| order_request_id | bigint | FK → order_requests |
| user_id | bigint | 承認者のユーザーID |
| status | int | 承認状態（null:未到達, 0:承認待ち, 1:承認, 2:非承認） |
| final_flg | boolean | 最終承認者フラグ |
| comment | text | コメント履歴（日付・ステータス付き） |

#### stock_storages（在庫格納先）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_id | bigint | FK → stocks |
| storage_address_id | bigint | FK → storage_addresses |
| quantity | int | 在庫数量 |
| reorder_point | decimal | 発注点（自動計算） |

#### stock_suppliers（物品-仕入先紐付）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_id | bigint | FK → stocks |
| supplier_id | bigint | FK → suppliers |
| main_flg | int | 主要仕入先フラグ |
| lead_time | int | リードタイム（日） |
| postage | decimal | 送料 |
| memo | text | メモ |

#### stock_supplier_prices（仕入先価格マスタ）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_supplier_id | bigint | FK → stock_suppliers |
| price | decimal | 単価 |
| start_date | date | 適用開始日 |
| end_date | date | 適用終了日 |
| active_flg | int | 有効フラグ |

#### stock_requests（現場物品依頼マスタ）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| stock_id | bigint | FK → stocks |
| alias | varchar | 表示用別名 |
| unit | varchar | 依頼単位 |
| orderNumber | int | 表示順 |

#### stock_request_orders（現場物品依頼明細）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| user_id | bigint | FK → users（依頼者） |
| process_id | bigint | FK → processes（工程・部署） |
| stock_id | bigint | FK → stocks |
| quantity | int | 依頼数量 |
| status | int | 0:未完了, 1:完了 |
| order_flg | int | 発注依頼済みフラグ |

#### initial_orders（発注実績）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| order_request_id | bigint | FK → order_requests |
| stock_id | bigint | FK → stocks |
| name | varchar | 品名 |
| s_name | varchar | 品番 |
| quantity | int | 発注数量 |
| price | decimal | 単価 |
| order_date | date | 発注日 |
| delifile_path | varchar | 納品書ファイルパス |
| receive_flg | int | 納品完了フラグ |
| receipt_flg | int | 受領完了フラグ |
| not_found_flg | int | 在庫マスタ未登録フラグ |
| none_storage_flg | int | 格納先なしフラグ |
| del_flg | int | 削除フラグ |
| deli_location | varchar | 納品場所 |
| com_name | varchar | 仕入先名 |

#### inventory_operation_records（在庫操作履歴）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| inventory_operation_id | int | 操作種別ID |
| stock_id | bigint | FK → stocks |
| stock_storage_id | bigint | FK → stock_storages |
| quantity | int | 操作数量 |
| bef_quantity | int | 操作前数量 |
| user_id | bigint | 操作者ID |

**inventory_operation_id の値:**

| 値 | 操作種別 |
|----|---------|
| 2 | 出庫（払出） |
| 7 | 発注点更新 |
| 8 | 納品入庫 |

#### documents（稟議書）

| カラム | 型 | 説明 |
|--------|-----|------|
| id | bigint | PK |
| title | varchar | タイトル |
| content | text | 内容 |
| main_reason | text | 主要理由 |
| sub_reason | text | 補足理由 |
| evalution_date | date | 評価日 |

### 4.2 マスタテーブル

| テーブル名 | 説明 |
|-----------|------|
| users | ユーザー（社員）マスタ |
| groups | グループ（部門）マスタ |
| positions | 役職マスタ |
| processes | 工程・部署マスタ |
| suppliers | 仕入先マスタ |
| classifications | 物品分類マスタ |
| locations | 格納場所（倉庫等）マスタ |
| storage_addresses | 格納先アドレス（棚番）マスタ |
| devices | タブレット端末マスタ |

### 4.3 通知関連テーブル

| テーブル名 | 説明 |
|-----------|------|
| notify_queues | 通知キュー |
| notify_queue_users | 通知対象ユーザー |
| notify_groups | 通知グループ |
| notify_group_users | 通知グループメンバー |
| device_messages | デバイスメッセージ |

---

## 5. ビジネスロジック詳細

### 5.1 発注依頼フロー

```
[現場タブレット] 物品依頼作成
    ↓
OrderController@store
    ├── 仕入先の有効価格を取得（StockSupplierPrice.validAt）
    ├── 合計金額を算出（単価 × 数量）
    ├── 使用済み価格のactive_flgを0に更新
    ├── 在庫マスタの単価を最新価格で更新
    ├── 発注点を再計算（移動平均）
    └── 通知キュー作成（発注担当者へ）
    ↓
[発注担当者] 発注内容確認・承認申請
    ↓
AcceptController（承認ワークフロー）
    ↓
[承認完了] → 発注確定
    ↓
[発注担当者] 仕入先へ発注
    ↓
ReceiveController（納品処理）
    ├── 納品書アップロード
    ├── 納品数量登録 → 在庫数量加算
    ├── 分納対応（SplitOrderQuantity）
    ├── 単価変動時の自動更新
    └── 端末・サイネージへ納品通知
```

### 5.2 承認ワークフロー詳細

**承認者一覧（ユーザーIDベース）:**

| ユーザーID | 氏名 | 役職 |
|-----------|------|------|
| 2 | （社長） | 社長 |
| 63 | （常務） | 常務 |
| 94 | （部長） | 部長 |
| 36 | （部長） | 部長 |
| 16 | 梶谷 | — |
| 37 | 長谷川 | — |
| 84 | 宮原 | — |
| 144 | 原 | — |
| 18 | 繁田 | — |

**承認処理フロー:**

```
承認待ち(status=0) → 承認(status=1)
    ├── final_flg=1 → accept_flg=2（最終承認完了）→ 依頼者へ通知
    └── final_flg=0 → 次の承認者のstatusを0に設定 → 次の承認者へ通知

承認待ち(status=0) → 非承認(status=2)
    ├── 前の承認者が存在 → accept_flg=6（差し戻し）→ 前の承認者へ通知
    └── 前の承認者なし（第一承認者が却下）→ accept_flg=3（却下）→ 依頼者へ通知
```

**コメント履歴フォーマット:**
```
YY/MM/DD [承認/非承認]
コメント内容
----------------------
```

**緊急度レベル（承認画面表示用）:**

| レベル | 条件 |
|--------|------|
| 0（通常） | リードタイム到来日まで4日以上 |
| 1（間近） | リードタイム到来日まで3日以内 |
| 2（期限切れ） | リードタイム到来日が当日以前 |

### 5.3 現場物品依頼フロー

```
[現場作業者] 物品を選択・数量入力
    ↓
StockRequestController@store
    └── StockRequestOrder作成（process_id=工程, status=0）
    ↓
[倉庫担当] 依頼確認・払出
    ↓
StockRequestController@complete
    ├── StockStorage.quantity を減算後の値に更新
    ├── StockRequestOrder.status = 1（完了）
    └── InventoryOperationRecord作成（type=2:出庫）
    ↓
[在庫不足の場合]
StockRequestController@order
    ├── OrderRequest自動作成（数量=1, 在庫マスタ単価で）
    ├── 発注点再計算
    └── 未完了の同一物品依頼のorder_flg=1に更新
```

### 5.4 納品処理フロー

```
[発注済み] initial_orders テーブルに記録
    ↓
[ステップ1] 納品書アップロード（ReceiveController@uploadFile）
    ├── ファイル保存: storage/app/public/deli_file/{timestamp}.{ext}
    ├── initial_orders.delifile_path に設定
    └── リードタイム再設定（Method::setDeliveryDateAndUpdateLeadTime）
    ↓
[ステップ2] 納品数量登録（ReceiveController@updateDelivery）
    ├── 全量納品 → receive_flg=1, 在庫加算
    ├── 分納 → SplitOrderQuantity作成, 部分在庫加算
    ├── 換算対応（conversion_flg）: 梱包入数×発注数→実数量
    ├── 単価変更時 → Stock.price更新 + StockPriceArchive記録
    └── 端末通知（DeviceMessage + Firebase FCM）
    ↓
[ステップ3] 受領確認（ReceiveController@updateReceipt）
    └── receipt_flg=1（現場への引き渡し完了）
```

### 5.5 発注点（リオーダーポイント）自動計算

```php
// Helper::updateReOrderPoint()
// 発注依頼時の在庫数量を記録 → 移動平均で発注点を算出

InventoryOperationRecord(type=7) に bef_quantity（発注時在庫数量）を記録
    ↓
同一格納先の type=7 レコードの bef_quantity の平均値を算出
    ↓
StockStorage.reorder_point に設定
```

### 5.6 価格管理

```
[価格の優先順位]
1. StockSupplierPrice（期間別仕入先価格）← 有効期間内 + active_flg=1
2. Stock.price（物品マスタの基本価格）

[発注時の価格処理]
- 希望納期時点で有効な StockSupplierPrice を取得
- 取得した価格の active_flg を 0 に更新（使用済み）
- 物品マスタの price を最新価格で上書き

[納品時の価格処理]
- 発注時単価と異なる場合 → Stock.price を更新
- StockPriceArchive に変更履歴を記録
```

---

## 6. 通知システム

### 6.1 通知チャネル

| チャネル | 実装 | 用途 |
|---------|------|------|
| Teams Webhook | Helper::sendNotify() | 承認依頼・承認結果・却下通知 |
| 内部通知キュー | Helper::createNotifyQueue() | アプリ内通知（ポーリング取得） |
| Firebase FCM | Helper::sendNotification() | タブレット端末へのプッシュ通知 |
| デバイスメッセージ | Helper::createDeviceMessage() | 端末間メッセージ（却下詳細・納品通知） |

### 6.2 通知タイミング

| イベント | 通知先 | チャネル |
|---------|--------|---------|
| 発注依頼作成 | 発注担当者（ID:68,48 or 39） | 内部通知キュー |
| 承認依頼 | 次の承認者 | 内部通知キュー |
| 最終承認完了 | 依頼者 | 内部通知キュー + FCM |
| 却下（第一承認者） | 発注担当者 + 依頼者 + 端末 | 内部通知キュー + FCM + デバイスメッセージ |
| 差し戻し | 前の承認者 | 内部通知キュー |
| 納品完了 | 依頼元端末 | デバイスメッセージ + FCM |

### 6.3 分類別の発注担当者

| 分類 | classification_id | 担当者 | ユーザーID |
|------|------------------|--------|-----------|
| 原材料・副資材 | 34 | （指定担当者） | 39 |
| その他全般 | 34以外 | 岡堂・中村 | 68, 48 |

---

## 7. 外部API連携

### 7.1 Conservation API（外部在庫連携）

| エンドポイント | メソッド | 説明 |
|--------------|---------|------|
| `/api/stocks/{id}` | GET/POST/PUT/PATCH/DELETE | 在庫マスタCRUD |
| `/api/stock-storages/subtract` | POST | 在庫数量減算 |
| `/api/stock-storages/add` | POST | 在庫数量加算 |
| `/api/stock-storages/{id}` | PUT | 在庫数量直接更新 |
| `/api/users/{id}` | GET | ユーザー情報取得 |

### 7.2 ChatGPT API

| エンドポイント | メソッド | 説明 |
|--------------|---------|------|
| `/api/chatgpt` | POST | ChatGPT APIへの問い合わせ |

### 7.3 Firebase

- **プロジェクトID**: akioka-cloud-notify-service
- **認証情報**: storage/app/public/firebase/firebase-credentials.json
- **用途**: タブレット端末へのプッシュ通知（FCM v1 API）

---

## 8. ファイル保存先

| 種別 | パス | 説明 |
|------|------|------|
| 稟議書 | storage/app/public/order_request/{YmdHis}.pdf | 発注依頼の添付資料 |
| 納品書 | storage/app/public/deli_file/{timestamp}.{ext} | 納品書画像・PDF |
| 在庫画像 | storage/app/public/ | 物品の写真 |
| Firebase認証 | storage/app/public/firebase/firebase-credentials.json | FCM認証キー |

---

## 9. 認証・認可

### 9.1 認証方式

- **Web**: Laravel Breeze（セッションベース）
- **API**: Laravel Sanctum（トークンベース）

### 9.2 アクセス制御

- 承認画面: ユーザーIDベースのホワイトリスト（AcceptController内でハードコード）
- タブレット端末: デバイスIDベースの識別
- 一般画面: 認証なし（社内ネットワーク前提）

---

## 10. 技術的な注意事項

### 10.1 既知の設計特性

1. **承認者のハードコード**: AcceptController内でユーザーIDが直接指定されている。承認者の追加・変更時はコード修正が必要。
2. **発注担当者のハードコード**: OrderController内で通知先ユーザーID（68, 48, 39）が直接指定されている。
3. **ユーザーID 117**: 依頼者未指定時のデフォルトユーザー（request_user_id=0の場合）。
4. **ユーザーID 120**: 現場物品依頼の払出記録で固定の操作者として使用。
5. **分類ID 34**: 「原材料・副資材」カテゴリとして特別扱い（担当者・承認フローが異なる）。

### 10.2 フロントエンド構成

- Inertia.jsによるSPA構成
- 主要ページコンポーネント:
  - `Accept/OrderRequest` — 承認画面
  - `Stock/Request/Home` — 現場物品依頼
  - `Stock/Receive/*` — 納品管理（Index, Archive, Receipt, Delivery）
  - `Stock/NewItem/*` — 新規品依頼

### 10.3 PWA対応

- `manifest.json` による PWA メタデータ
- `sw.js` による Service Worker
- タブレット端末でのオフライン対応を想定

---

## 11. 環境情報

### 11.1 データベース接続

| 項目 | 値 |
|------|-----|
| ホスト | localhost |
| ポート | 3308 |
| データベース名 | akioka_db |
| ユーザー名 | akioka_administrator |

### 11.2 外部サービス

| サービス | 用途 |
|---------|------|
| Microsoft Teams Webhook | 社内通知 |
| Firebase Cloud Messaging | プッシュ通知 |
| ChatGPT API | AI問い合わせ機能 |

---

## 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-04-08 | 初版作成（既存コードからのリバースエンジニアリング） |
